{
  "name": "margin-llm-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Serverless proxy that holds the Anthropic API key and generates AI definitions for the Margin app.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.112.4"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
